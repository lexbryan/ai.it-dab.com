export const metadata = {
  title: "DAB AI Gateway",
  description: "Admin UI for the DAB AI gateway (placeholder scaffold).",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}

export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>DAB AI Gateway</h1>
      <p>
        Admin console for API keys and personas. Sign-in and key management
        arrive in later tickets.
      </p>
    </main>
  );
}

export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>DAB AI Gateway</h1>
      <p>
        Frontend placeholder. The admin UI for API keys and personas, the test
        runner, and the shared API client arrive in later tickets.
      </p>
    </main>
  );
}

// Package config will load and validate structured configuration from the
// environment (backed by an .env.example contract).
//
// Placeholder: the implementation is delivered in a later ticket. It exists now
// so the package layout every other ticket imports is in place.
package config
